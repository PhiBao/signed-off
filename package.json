{
  "name": "signedoff",
  "version": "0.1.0",
  "private": true,
  "description": "Attach client-readable proof of delivery to freelance web milestones.",
  "license": "MIT",
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "demo": "bash scripts/demo.sh",
    "signedoff": "pnpm --filter @signedoff/cli exec tsx src/index.ts",
    "typecheck": "pnpm -r --parallel typecheck",
    "test": "pnpm --filter @signedoff/cli test",
    "build": "pnpm -r --parallel build"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "tsx": "4.23.12",
    "typescript": "7.0.2"
  }
}
